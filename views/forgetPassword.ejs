<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;700&display=swap" rel="stylesheet"> <!-- Include Poppins font -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css"> <!-- Include Font Awesome for icons -->
    <link rel="stylesheet" href="/styles/style.css"> <!-- Include your CSS file here -->
    <link rel="icon" href="/photos/lights_out_icon.jpeg" type="image/jpeg">

    <style>
        /* Add your custom styles here */
        body {
            font-family: 'Poppins', sans-serif;
            background-color: #f9f9f9;
            margin: 0;
            padding: 0;
            display: flex;
            flex-direction: column;
            align-items: center;
            min-height: 100vh;
        }

        header {
            width: 100%;
            background-color: #fa32a0; /* Light pink background color */
            padding: 20px 0;
            text-align: center;
            color: #fff; /* White text color */
            font-size: 24px;
            font-weight: 700;
            text-transform: uppercase;
            border-bottom: 2px solid #ff7eb9; /* Dark pink border bottom */
        }

        .form-container {
            background-color: #fff;
            padding: 40px;
            border-radius: 20px;
            box-shadow: 0 10px 20px rgba(0, 0, 0, 0.1);
            text-align: center;
            max-width: 500px;
            width: 100%;
            position: relative;
            overflow: hidden;
            margin-top: 20px;
        }

        .form-container:before {
            content: '';
            position: absolute;
            top: -20px;
            left: -20px;
            width: 50%;
            height: 100%;
            background: linear-gradient(to bottom right, #2ecc71, #27ae60);
            z-index: -1;
            border-radius: 0 0 50% 50%;
            transition: all 0.4s ease;
        }

        h2 {
            margin-bottom: 20px;
            color: #333;
            font-size: 36px;
            font-weight: 700;
            text-transform: uppercase;
        }

        h3 {
            margin-bottom: 20px;
            color: #555;
            font-size: 24px;
            font-weight: 700;
        }

        input[type="text"],
        input[type="email"],
        input[type="password"] {
            width: calc(100% - 32px);
            padding: 16px;
            margin-bottom: 20px;
            border: none;
            border-radius: 8px;
            box-sizing: border-box;
            background-color: #f5f5f5;
            font-family: 'Poppins', sans-serif;
            transition: all 0.3s ease;
        }

        #submit {
            width: calc(100% - 32px);
            background-color: #2ecc71;
            color: white;
            padding: 16px;
            margin: 8px 0;
            border: none;
            border-radius: 8px;
            cursor: pointer;
            transition: all 0.3s ease;
        }

        #submit:hover {
            background-color: #27ae60;
            transform: translateY(-3px);
            box-shadow: 0 5px 15px rgba(46, 204, 113, 0.4);
        }

        p {
            margin-top: 20px;
            color: #666;
        }

        a {
            color: #2ecc71;
            text-decoration: none;
            font-weight: 700;
        }

        a:hover {
            text-decoration: underline;
        }

        .home-button {
            position: absolute;
            top: 20px;
            right: 20px;
            background-color: #fa32a0; /* Light pink background color */
            border: none;
            font-size: 20px;
            color: #fff; /* White text color */
            cursor: pointer;
            padding: 10px 20px; /* Added padding to make button visible */
            border-radius: 8px; /* Added border-radius for rounded corners */
            text-transform: uppercase; /* Uppercase text */
        }

        .home-button:hover {
            text-decoration: underline;
        }
    </style>
    <title>Forgot Password</title>
</head>

<body>
    <header>
        Lights Out
    </header>
    <button class="home-button" onclick="window.location.href='/';">Home</button>

    <% if (page === 'enterEmail') { %>
        <div class="form-container">
            <h2>Forgot Password</h2>
            <h3>Enter Your Registered Email</h3>
            <% if(locals.message) { %>
<p id="message" class="text-red-500">
    <%=message %>
</p>
           <% } %>
            <form action="/forgot-password" method="post">
                <input type="email" name="email" placeholder="Email" required><br>
                <input id="submit" type="submit" value="Send OTP">
            </form>
            <p>Remembered your password? <a href="/login">Login</a></p>
        </div>
    <% } else if (page === 'enterOTP') { %>
        <div class="form-container">
            <h2>Forgot Password</h2>
            <h3>Enter OTP</h3>
            <% if(locals.message) { %>
                <p id="message" class="text-red-500">
                    <%=message %>
                </p>
                           <% } %>
            <form action="/enter-otp" method="post">
                <input type="hidden" name="email" value="<%= email %>">
                <input type="text" name="enteredOTP" placeholder="OTP" required><br>
                <input id="submit" type="submit" value="Verify OTP">
            </form>
            <form action="/resend-otp" method="post">
                <input type="hidden" name="email" value="<%= email %>">
                <p>Didn't receive OTP? <input class="text-green-500 cursor-pointer hover:underline" type="submit" value="Resend OTP"></p>
            </form>
        </div>
    <% } else if (page === 'resetPassword') { %>
        <div class="form-container">
            <h2>Forgot Password</h2>
            <h3>Reset Your Password</h3>
            <form action="/reset-password" method="post">
                <input type="hidden" name="email" value="<%= email %>">
                <input type="password" name="password" placeholder="New Password" required><br>
                <input type="password" name="confirmPassword" placeholder="Confirm Password" required><br>
                <input id="submit" type="submit" value="Reset Password">
            </form>
            <p>Remembered your password? <a href="/login">Login</a></p>
        </div>
    <% } %>
</body>
<script>
    setTimeout(() => {
        document.getElementById("message").classList.add("hidden");
        
    }, 3);
</script>

</html>
